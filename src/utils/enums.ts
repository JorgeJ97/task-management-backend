export enum CategoryTypes{
    PERSONAL = 'personal',
    WORK = 'work',
    URGENT = 'urgent',
    REMINDER = 'reminder',
    GENERAL = 'general'
}

export enum PriorityLevels {
    LOW = 'low',
    MEDIUM = 'medium',
    HIGH = 'high'
} 